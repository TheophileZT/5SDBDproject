name: Fetch Bikes and Save to MongoDB

on:
  schedule:
    # Planification toutes les 15 minutes
    - cron: "15 * * * *"

jobs:
  fetch-and-save:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13.1' 
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
            pip install -r requirements.txt

      - name: Run bikes_script.py
        run: python scripts/fetch_events.py Toulouse --mongo_uri mongodb+srv://theozt25:MGZ7Osyw7gMrGU4O@integratorproject.5ulkz.mongodb.net/ --db DB --collection Bikes