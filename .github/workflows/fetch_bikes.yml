name: Fetch Bikes and Save to MongoDB

on:
  workflow_dispatch

jobs:
  fetch_bikes:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13.1' 
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
            pip install -r requirements.txt

      - name: Run bike_script.py
        run: python scripts/bike_script.py Toulouse --mongo_uri mongodb+srv://theozt25:MGZ7Osyw7gMrGU4O@integratorproject.5ulkz.mongodb.net/ --db DB --collection Bikes
